'use client';

import React from 'react';

import { Section } from '@/components/shared/section';

export const Modal: React.FC = () => {
    return (
        <Section>
          
        </Section>
    );
};